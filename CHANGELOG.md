# CHANGELOG

Let's Meet in FF14의 주목할 만한 변경사항.
형식: [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 변형 (한국어 섹션).

각 항목 prefix:
- **신규** (Added) — 새 기능
- **변경** (Changed) — 기존 동작 변경
- **수정** (Fixed) — 버그 수정
- **제거** (Removed) — 삭제된 기능

날짜는 한국 시간(KST) 기준.

---

## [Unreleased]

### 신규
- **헤더 버그리포트 바로가기**: (app) 헤더의 계정 메뉴 왼쪽에 버그리포트 아이콘 버튼 추가 (`/bugs`로 이동). 기존엔 홈 화면 카드로만 진입 가능했음.
- **버그리포트 로그 첨부**: 새 리포트 작성 시 "로그 첨부" 체크(기본 켜짐) 시 현재 URL·브라우저 정보·화면 크기·온라인 상태·최근 콘솔 에러(최대 30줄)를 함께 제출. `AuthProvider`가 앱 시작 시 `console.error`/`warn` + 미처리 예외를 링버퍼로 수집(`lib/logging/consoleBuffer.ts`), 서버로는 절대 자동 전송하지 않고 리포트 제출 시에만 스냅샷을 담음. 상세 페이지에서 첨부 로그는 작성자 본인과 운영자에게만 접힌 상태로 노출 — 다른 열람자에겐 비공개.
- **사이트 공지사항 게시판**: 운영자가 사이트 전체 공지를 작성 (`/announcements`). 비로그인 사용자도 열람 가능. 상단 고정 기능. 운영자 권한은 환경변수 `ADMIN_UIDS` (Firebase Auth uid 쉼표 구분) 화이트리스트로 판정 (`lib/auth/admin.ts`). 모든 쓰기는 Admin SDK 경유 (`POST /api/announcements`, `PATCH/DELETE /api/announcements/[id]`). Firestore Rules는 클라이언트 직접 쓰기를 막고 read만 허용.
- **사용자 버그리포트 게시판**: 로그인 사용자가 오류/개선 제안을 등록 (`/bugs`). 다른 로그인 사용자도 열람 가능 (중복 리포트 방지). 상태(접수/확인 중/해결) + 운영자 답변. 본인 글은 직접 수정·삭제, 운영자는 모든 글에 대해 상태 변경/답변/삭제 가능 (`PATCH/DELETE /api/bugs/[id]`). Firestore Rules: 작성자 본인은 title/body만 수정 가능, status·reply 변경은 Admin SDK 경유 강제.
- **홈 화면 게시판 노출**: 홈에 공지사항 최근 3개 인라인 + 버그리포트 진입 카드 추가. 기존 "오류 및 개선사항 접수: 비누솝@톤베리" 텍스트 안내는 새 게시판으로 대체. 비로그인 상태에서 버그리포트 카드 클릭 시 로그인 페이지로 우회 (`next=/bugs`).
- **운영자 판정 API + 훅**: `GET /api/me/admin`이 현재 사용자의 admin 여부를 반환, 클라이언트 `useAdmin()` 훅에서 사용 (운영자 전용 UI 노출 제어). 실제 권한 체크는 항상 서버에서 재검증.
- **일정 조율 방식 선택 (요일만 고르기)**: 공대장이 공대 생성/수정 시 일정 조율 방식을 고를 수 있음.
  - `timeGrid` (기존) — 30분 슬롯 그리드로 가능 시간 입력.
  - `dayOnly` (신규) — 레이드 시간을 고정(시작~종료)하고, 공대원은 가능한 요일만 토글. 입력 화면은 요일 7개 세로 리스트 (본인 선택=파랑, 다른 공대원 수=초록 농도, 출발 가능 요일=외곽선). 결과 모드는 요일별 출발 가능/불가 + 행 펼쳐 가능자 명단.
  - 고정 시간은 1릴 길이의 정수배만 허용 (예: 영식 90분 → 21:30~23:00). 종료가 시작보다 빠르면 자정 넘김으로 해석.
  - 매칭 엔진은 무변경 — dayOnly는 고정 윈도우의 슬롯을 aligned 분할해 기존 1릴 판정/추천/일정 확정 흐름을 그대로 사용.
  - 일정 조율 방식은 공대장이 언제든 변경 가능. 두 방식 모두 같은 슬롯 데이터를 쓰므로 기존 응답은 지워지지 않고 새 방식 화면에 그대로 반영됨 (시간별→요일별 전환 시, 그 요일 시간대 전체가 가능한 사람만 가능 요일로 표시). 수정 폼에서 방식을 바꾸면 방향별 안내 문구 노출.
- **세션 쿠키 자동 복구**: 로그인 직후 서버가 httpOnly `ff_session` 세션 쿠키를 발급. 클라이언트 IndexedDB 세션이 사라져도(디스코드 인앱 브라우저 등 웹뷰에서 저장소 휘발) 재방문 시 이 쿠키로 custom token을 재발급해 디스코드 재로그인 없이 조용히 복구. `/api/auth/session` (POST 발급 / GET 복구), `AuthProvider`가 세션 부재 시 1회 복구 시도 + rolling 갱신.
- **인앱 브라우저 안내**: 로그인 페이지에서 User-Agent로 디스코드 등 인앱 브라우저를 감지하면 "외부 브라우저로 열기" 안내 + 주소 복사 버튼 표시. 인앱 웹뷰는 로그인 유지가 불안정하므로 기본 브라우저 사용을 유도.
- **공대장 권한 이양**: 멤버 모달에 [공대장 양도] 버튼 추가 (리더 시점, 본인/다른 리더/프로필 미설정 멤버 제외). 양도 시 본인은 일반 공대원으로 변경. `/api/parties/transfer` (atomic batch).
- **출석 멤버 상세**: 일정 카드의 카운트("출 5 · 결 1 · 미정 2 · 무응답 N") 클릭 시 펼쳐서 누가 어느 상태인지 명단 표시. 결석 멤버는 사유도 같이.
- **모바일 (≤640px) 그리드 최적화**: 셀 높이 18→16px, 행/열 gap 축소, 컬럼 헤더/시간 라벨 폰트 한 단계 다운, 모바일에선 hover 툴팁 비활성, 셀 `touch-action: none`으로 드래그 중 페이지 스크롤 방지.
- **공대장 강퇴 기능**: 공대원 모달에서 리더에게만 [강퇴] 버튼 노출 (본인/다른 리더 제외). 확인 다이얼로그 → 멤버 doc 삭제 + 사용자 partyIds에서 제거. `/api/parties/kick` (서버 라우트, leader 권한 검증).
- **공대 해체 (리더)**: 좌측 패널 하단에 [공대 해체] 버튼. 멤버·가능 시간·일정·출석·메모 모두 삭제 + 모든 사용자의 partyIds에서 제거 + 공대 doc 삭제. `/api/parties/disband` 단일 batch.
- **첫 프로필 설정 강제**: 가입/공대 생성 시 placeholder(Moogle/WAR/MT) 그대로 저장되던 문제 → `Member.profileSetup` 플래그 도입. 첫 진입 시 서버·메인 잡·메인 자리는 빈 상태로 시작, 사용자가 의식적으로 선택 후에만 저장 가능. 필수 필드엔 빨강 `*` 표시.
- **프로필 미설정 표시**: profileSetup=false인 공대원은 멤버 목록·내 프로필·상세 모달에서 "프로필 미설정"으로 노출 (placeholder인 모그리/전사/MT를 잘못 노출하지 않게).
- **계정 삭제 (PIPA 회원탈퇴권)**: 헤더 우상단 디스코드 프로필 클릭 → 드롭다운 메뉴에 [로그아웃] / [계정 삭제]. 삭제 시 모든 공대 자동 정리 (리더면 공대 해체, 멤버면 self-kick) + Firestore users 삭제 + Firebase Auth user 삭제. `/api/account/delete`.

### 변경
- **공대원 상세 모달 요일 표 정리**: dayOnly 공대에서 공대원 프로필 모달의 read-only 요일 표는 그 멤버의 선택 요일만 보여주므로, 의미 없이 항상 떠 있던 "다른 공대원 없음" 텍스트를 숨김 (`DayOnlyGrid` `hideOthers` prop).
- **가입 링크 복사 + 초대 메시지**: 공대 정보 패널의 [가입 링크]를 누르면 링크로 이동하던 동작 → 클립보드에 초대 메시지 복사로 변경. 메시지 형식 `"{공대장 캐릭터명}님의 {공대명}에 참가하세요. {링크}"`. 복사 직후 버튼 라벨이 "초대 메시지 복사됨!"으로 잠시 바뀜.
- **초대 링크 진입 시 자동 분기**: `/join?code=XXX`로 들어왔을 때 이미 그 공대 멤버라면 가입 폼을 건너뛰고 바로 공대 페이지로 이동. `GET /api/parties/join?code=` (charName 없이 멤버 여부만 확인)로 판별.
- **공대 정보 패널 다듬기**: (1) 긴 공대 이름이 `…`로 잘리던 것 → 줄바꿈으로 표시. (2) 공대원 목록을 자리 순서(MT·ST·MH·SH·D1~D4)로 정렬, 프로필 미설정 멤버는 뒤로. (3) dayOnly 고정 시간을 공대 이름 아래에 빨간 글자로 강조 (연한 회색 → 가시성 개선). (4) [공대 해체] 버튼을 빨강 채움 → 빨강 테두리로 변경 (실수로 누르고 싶어지는 것 완화).
- **편집 중 셀 툴팁 완전 비활성**: 모바일에서 hover가 입력에 끌려가 거슬려 시간 표시도 숨김. 잠금 상태(제출 후)에선 그대로 명단 노출.
- **input/textarea 내부 스크롤바·resize 핸들 숨김**: 입력 시 스크롤바가 살짝 노출되던 케이스 차단. body 스크롤바도 함께 숨김 처리.
- **공대원 모달 스크롤바 숨김**: 모달 내부가 길어 세로 스크롤이 생길 때 바를 보이지 않게 (no-scrollbar 클래스).
- **헤더 사용자 영역**: 단순 닉네임 + 로그아웃 텍스트 → 드롭다운 메뉴(UserMenu)로 통합. 로그아웃·계정 삭제가 이 메뉴 안에.
- **추천 카드 빈 상태 문구**: 응답 부족 시 안내 문구를 "(N릴) 출발 가능한 시간이 아직 없습니다…"로 정리 — 1릴 표기를 괄호로 묶고 "연속" 단어를 제거해 가독성 개선.

### 수정
- **비가입자가 공대 링크 접근 시 오류 메시지 개선**: `/party/{id}`를 비멤버가 열면 Firestore 룰이 read를 거절해 "공대 정보를 불러오지 못했습니다"라는 원인 불명의 오류만 뜨던 문제. `permission-denied`를 구분해 "가입되지 않은 공대입니다" 안내 + [초대 코드로 가입하기](`/join`) 버튼으로 대체. (참고: 공대 링크는 원래부터 `/party/{공대ID}` 하나뿐 — 별도의 "가입 전용 링크"는 `/join?code=` 초대 코드 링크이며 기존에도 존재. 문제는 URL 체계가 아니라 실패 시 안내 문구였음.)
- **8인 공대에서 프로필 미설정 멤버 1명만 있어도 "출발 가능"이 영원히 안 뜨던 문제**: 매칭 로직이 profileSetup=false 멤버를 대상에서 제외하고 있었는데, 정원이 정확히 8명인 공대에서는 1명만 빠져도 매칭 가능 인원이 7명이 되어 나머지 전원이 완벽히 응답해도 8자리를 채울 수 없었음(실제 운영 데이터에서 재현·확인). "프로필 미설정 멤버도 placeholder 자리로 매칭 대상에 포함"하도록 정책을 바꿔 해결 — 대신 2명 이상이 동시에 미설정 상태면 기본값(MT)이 겹쳐 그 자리에서 매칭이 실패할 수 있음(정상적인 "자리 안 맞음" 판정). 이 과정에서 화면 응답 카운트와 실제 매칭 대상이 어긋나던 표시 버그도 함께 정리 (`AvailabilityPanel`).
- **`/party/{id}` URL에 초대 코드를 잘못 입력하면 "가입되지 않은 공대입니다"만 뜨던 문제**: 공대 ID(8자)와 초대 코드(6자)를 혼동해 `/party/{초대코드}`로 접속하는 사례가 반복 확인됨. 이제 6자 값이 들어오면 자동으로 `/join?code={값}`으로 리다이렉트 — 이미 멤버면 바로 공대 페이지로, 아직이면 코드가 채워진 가입 폼으로 이동 (`app/(app)/party/[id]/page.tsx`).

### 보안
- **fflogsUrl URL 형식 검증**: 클라이언트 폼에서 `^https?://` 강제 + Firestore 룰로도 동시 검증. 렌더링 시 `safeHttpUrl` 헬퍼로 한 번 더 (XSS 방어).
- **users/{uid} 읽기 권한 강화**: 누구나 read → 본인만 read. 다른 사용자 정보(닉네임·아바타)는 멤버 doc(charName 등)으로 충분.
- **공대 생성 수 제한**: Firestore 룰에서 한 사용자 최대 10개 공대까지만 생성 가능 (`partyIds.size() < 10`). 스팸/저장소 폭증 방어.
- **시간 라벨 가독성**: 시간 그리드 좌측 시간 라벨 폰트 키움(10→15) + 색 한 단계 진하게.

### 인프라
- **`.gitattributes` 추가**: `* text=auto eol=lf` — Windows 작업 시 매 commit CRLF 경고 차단.
- **`npm audit` 검토**: 프로덕션 의존성 중 high 2건 (Next 14.2 image optimizer DoS / undici random) 확인 — 호스팅 환경(Vercel)·코드 사용 패턴상 영향 적음. dev deps의 firebase-tools 관련 22건은 빌드 환경 한정.

---

## 2026-04-29 — Phase 1 배포 후 개선

### 신규
- **공대원 상세 모달** — 좌측 패널에서 멤버 li 클릭 시 모달. 프로필(직업·자리·프프로그·자기소개) + 본인이 입력한 이번 주 가능 시간 그리드(read-only).
- **셀 hover 툴팁** — 시간 그리드 셀에 마우스 올리면 시간 + "가능 (N명): 캐릭A, 캐릭B, ..." 표시. CSS only (data-tooltip).
- **메인 자리 중복 방지** — 프로필 편집 시 다른 공대원이 이미 main으로 잡은 자리는 비활성. 체인지 가능 자리에는 제약 없음.
- **OAuth redirect 보존** — `/join?code=XXX` 같은 URL을 비로그인 상태에서 접속 → Discord 로그인 → 자동으로 원래 URL로 회귀. open redirect 방어 포함.
- **멤버 제출 상태 시각 표시** — 좌측 멤버 목록에서 이번 주 응답 완료자=초록 틴트(25%), 미응답=빨강 틴트(25%) + "일정 입력 전" 배지.
- **세션 1릴 개수 옵션** — 공대 만들기/수정 시 한 세션에 진행할 1릴 개수(1~4) 선택. 추천 카드/일정 확정 길이가 이 값 기준.
- **공대원 8명 정원 한도** — 9번째 가입 시도 시 서버에서 거부.
- **공대원 실시간 갱신** — 다른 사람 가입/탈퇴/프로필 변경이 본인 화면에 즉시 반영 (onSnapshot).
- **다크/라이트 테마 토글** — 헤더 우상단 ☀️/🌙. 다크 기본, 선택 localStorage 저장.

### 변경
- **이름**: FFTuning → Let's Meet in FF14
- **레이드 콘텐츠**: 모든 극만신 40개 추가 (신생~황금), 환만신 제거, 영식 이름 약칭화 ("아르카디아 헤비급 1식" → shortKor "헤비급 1층"), nameKor는 풀네임 유지
- **드롭다운 정렬**: 극만신 → 영식 → 절 (각 그룹 내 패치 내림차순)
- **공대 기본명 로직**: 극만신은 풀네임 fallback ("극 에누오 공대"), 토벌전 접미사 제거
- **모든 input placeholder 제거**

### 수정
- **시간 그리드 클릭 화면 갱신 안 되는 버그** — 제출 전 상태에서 selfSlots가 savedSlots(빈 값)를 가리키던 문제. editable 기준으로 draft/saved 분기.
- **다른 사람 가입이 즉시 반영 안 되던 문제** — listPartyMembers (1회 fetch) → subscribePartyMembers (onSnapshot)
- **Vercel 배포 ESLint 차단** — unused vars/imports 정리
- **Discord OAuth localhost redirect 문제** — 환경변수 + Vercel redeploy 필요 안내
- **잡 아이콘 매핑** — XIVAPI 잡 ID 6개 off-by-one 보정 (BLU 누락분)
- **Next.js 14.2.35 보안 패치 반영**

### 인프라
- **Firestore 룰**: 일반 멤버가 progressNote 단일 필드만 update 가능 (협업 메모용)
- **App Hosting → Vercel**: 배포 플랫폼 전환
- **GitHub repo private**: boriborisal/letsmeetinff14

---

## 2026-04-28 — Phase 1 MVP 첫 배포

### 신규 (초기 커밋)
- **인증**: Discord OAuth (Firebase Custom Token 방식). 로그인/로그아웃, users/{uid} 자동 생성.
- **공대 관리**: 생성 / 수정 / 탈퇴 / 초대 코드 / 8명까지 가입.
- **멤버 프로필**: 캐릭명, 서버(모그리/초코보/카벙클/톤베리/펜리르), 메인 잡 + 가능 잡, 메인 자리 + 체인지 가능 자리, 프프로그 URL, 자기소개.
- **가능 시간 입력 그리드**: when2meet 스타일, 30분 단위, 1릴 단위 wrap (셀 4/3/2개), 주 페이지네이션, 다른 공대원 응답 heatmap, 클릭/드래그 토글.
- **출발 가능 1릴 자동 추출**: 8자리 백트래킹 매칭, 연속 1릴 그룹화, 추천 카드 (가장 긴 그룹 + 주말 우선).
- **일정 확정 + 출석**: 공대장 확정, 멤버 출/결/미정 + 결석 사유 메모, 휴공 처리.
- **진도 메모**: 공대 페이지 자유 텍스트 (모든 멤버 편집).
- **레이드 시드**: 절 7 + 영식 60.
- **잡 아이콘**: XIVAPI 21개 다운로드, 롤 색 배경 위에 합성.

### 인프라
- Next.js 14 (App Router) + TypeScript + Tailwind
- Firebase (Firestore + Auth, asia-northeast3) + Discord OAuth
- Firestore 룰 + App Check 가이드
- Vercel 배포
