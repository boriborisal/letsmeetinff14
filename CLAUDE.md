# CLAUDE.md

FF14 한섭 레이드 공대용 **일정 조율 + 출석 운영 웹사이트** (Let's Meet in FF14).

## 프로젝트 정체성

**한 줄 포지셔닝**: "각자 가능 시간 입력하면 출발 가능한 1릴 슬롯이 자동으로 모인다."

- when2meet 스타일 시간 교집합 + FF14 도메인 (자리·1릴·롤 매칭) 결합.
- 프프로그 분석 도구 아님. 프프로그는 멤버 프로필 URL 텍스트만 저장.
- 그냥 반응형 웹사이트. PWA·네이티브 앱·푸시 X. 알림은 디스코드 봇으로만.

타겟: 한섭 절·영식 8인 공대. (이 프로젝트 컨텍스트에서 "영웅" = "영식"의 통칭. 신생 시절 영웅 난이도가 아님.)

## 핵심 흐름

```
1. 공대장: 레이드 선택 (/lib/raid/contents.ts에서 고름)
       ↓
2. 공대원 각자: 자리 선택 (메인 자리 + 체인지 가능 자리 무제한)
       ↓
3. 공대원 각자: 가능 시간 입력 (30분 슬롯 토글, 주 단위)
       ↓
4. 시스템: 교집합으로 "출발 가능 1릴 슬롯" 자동 추출
       ↓
5. 공대장: 후보 중 일정 확정
       ↓
6. 출석 체크 (확정 일정에 대해 출/결/미정, 변동 대응)
```

## 기술 스택

- **Frontend**: Next.js 14 (App Router), TypeScript, React
- **Styling**: Tailwind CSS, shadcn/ui
- **Backend/DB**: Firebase (Firestore + Auth), 리전 `asia-northeast3`
- **인증**: Discord OAuth (Firebase Auth Custom Token 방식)
- **알림**: Discord Bot (Phase 2부터, 별도 워커)
- **배포**: Vercel

**외부 API 자동 연동 일체 안 함**. fflogs/XIVAPI 미사용. 레이드 메타데이터는 코드 레벨 수동 관리.

## 도메인 지식 (반드시 숙지)

### 용어 정의 (혼동 금지)

- **공대원 (member)**: 정식 고정 멤버. 8인 레이드면 8명. 코드: `member`.
- **대타 (substitute / sub)**: 결석 시 외부 임시 인원. 코드: `substitute`. "헌터"는 안 씀.
- **자리 (slot)**: 8인 레이드의 8개 포지션. MT, ST, MH, SH, D1~D4. 코드: `slot`.
- **1음식 (food)**: 30분. 음식 버프 1회 지속 시간. 시간 그리드의 최소 단위.
- **1릴 (reel)**: 레이드 1회 트라이 묶음. 인스턴스 강제 퇴장 타이머 = 한 번에 트라이 가능한 최대 시간. 레이드 분류별 고정.
  - 절(絶) / 영식 4층 = **120분** (1릴 4슬롯)
  - 영식 1~3층 / 8인 일반 / 24인 연합 = **90분** (1릴 3슬롯)
  - 토벌전 / 극만신 = **60분** (1릴 2슬롯)
- **반릴**: 1릴의 절반. 절 = 60분, 영식 1~3층 = 45분 등. 짧은 일정 표기에 가끔 쓰지만 시스템에서 별도 처리하지 않음.

### FF14 한섭

- 서버 4개: **모그리, 초코보, 카벙클, 톤베리** (한국 DC 단일).
- 한섭은 글섭과 동시 패치. 시점 차이 없음.
- 주 제한 초기화: 매주 화요일 17:00.

### 8인 자리 시스템

- **MT** (Main Tank), **ST** (Sub Tank): 탱커 2자리
- **MH** (Main/Pure Healer, 백마/점성), **SH** (Sub/Shield Healer, 학자/현자): 힐러 2자리
- **D1, D2, D3, D4**: 딜러 4자리 (근접/원물/마법 자유 배치)

자리 = 직업 ≠ 1:1. 한 자리에 여러 직업 매핑 가능.
공대원은 메인 자리 1개 + 체인지 가능 자리 무제한 등록.

### 직업 분류

- **탱커**: 나이트(PLD), 전사(WAR), 암흑기사(DRK), 건브레이커(GNB)
- **힐러**: 백마도사(WHM), 학자(SCH), 점성술사(AST), 현자(SGE)
- **근접딜**: 몽크(MNK), 용기사(DRG), 닌자(NIN), 사무라이(SAM), 리퍼(RPR), 바이퍼(VPR)
- **원거리물리딜**: 음유시인(BRD), 기공사(MCH), 무도가(DNC)
- **마법딜**: 흑마도사(BLM), 소환사(SMN), 적마도사(RDM), 픽토맨서(PCT)

### 1릴 매핑 (레이드별 고정)

```ts
// /lib/raid/contents.ts 에서 RaidContent.tier로부터 자동 결정
const REEL_BY_TIER = {
  ultimate: 120,    // 절 시리즈
  savage_4: 120,    // 영식 4층
  savage_1_3: 90,   // 영식 1~3층
  normal_8: 90,     // 8인 일반 레이드
  alliance_24: 90,  // 24인 연합
  extreme: 60,      // 토벌전 / 극만신
};
```

**사용자가 1릴을 토글하지 않는다.** 레이드 선택 시 자동 적용. UI에선 정보로만 표시.

### 출발 가능 판정 (1릴 단위)

한 1릴 슬롯에서 "출발 가능" = **8자리 모두에 그 시간 가능한 공대원을 배치 가능**:

- 1릴 = N개 연속 30분 슬롯 (N = 절·영식4=4 / 영식1~3·24인=3 / 토벌전·극만신=2). 모두 응답해야 1릴 가능.
- 8자리에 공대원 배치 = 자리 매칭 문제 (한 자리에 한 명만, 한 명은 한 자리만).
- 공대원은 메인 자리 또는 체인지 가능 자리에 배치될 수 있음.
- 8명이라 brute force(최대 8! = 40320)로 충분.

### 본인 응답 시각화 모드

- **INPUT 모드**: 본인이 가능 시간 입력 중. 본인 응답 = 파랑, 다른 공대원 응답 = 초록 농도.
- **RESULT 모드**: 본인 제출 후. 본인 응답이 다른 공대원과 합쳐져서 통합 초록 농도. 출발 가능 1릴은 별도 강조 (외곽선).

## 디렉토리 구조

```
/app
  /(auth)             # 로그인 흐름
  /(app)              # 인증 후 메인
    /party/[id]       # 공대 페이지
    /availability     # 가능 시간 입력
    /result           # 교집합 결과 + 일정 확정
    /attendance       # 출석 체크
/components           # UI (shadcn/ui 기반)
/lib
  /firebase           # Firebase 클라이언트/어드민
  /discord            # Discord OAuth
  /raid               # 레이드 메타데이터 (코드 레벨 수동 관리)
  /matching           # 자리 매칭 + 1릴 출발 가능 판정 알고리즘
/types                # 공통 타입 정의
```

## 코드 규칙

- **타입은 `/types`에 모은다**. 도메인 타입(Job, Role, Slot, RaidContent, Member, Availability, ReelResult)은 한 파일에서 import.
- **Firestore 컬렉션 이름은 복수형 영문**: `parties`, `members`, `availabilities`, `schedules`, `attendances`, `announcements`, `bugReports`
- **한국어는 UI 텍스트에만**. 변수명·함수명·주석은 영어. 도메인 한국어 필드는 예외(`jobKor` 등).
- **민감 정보는 환경변수**: `DISCORD_CLIENT_SECRET`, `FIREBASE_ADMIN_KEY`, `ADMIN_UIDS`. `NEXT_PUBLIC_` 접두사 절대 금지.
- 컴포넌트는 기본 server component, 인터랙션 필요할 때만 `'use client'`.
- **반응형 웹**. PC와 모바일 둘 다 잘 보이게. 모바일은 380px 너비에서 검증.

## 자주 하는 실수 (피할 것)

- **자리(slot)와 직업(job)을 혼동하지 말 것**. 자리는 포지션 슬롯(MT/ST/MH/SH/D1~D4), 직업은 캐릭터 잡(WAR/WHM/SAM 등). 매핑은 N:N.
- **1릴은 사용자가 정하지 않는다**. `RaidContent.tier`에서 자동 결정.
- **출발 가능 판정은 단순 카운팅이 아니다**. "8명 응답"이 아니라 "8자리에 배치 가능"으로 검증해야 함. 한 사람이 두 자리 동시 채울 수 없음.
- **시간 슬롯은 30분 단위**. 1릴은 연속된 N슬롯(2/3/4) 모두 응답되어야 인정.
- **레이드 메타데이터 출처는 [`ff14_레이드_정리.md`](ff14_레이드_정리.md)**. 새 패치 시 이 MD를 먼저 갱신한 뒤 `/lib/raid/contents.ts` 동기화.
- **본인 응답 시각화는 두 모드** (INPUT/RESULT). 같은 그리드인데 색 처리 다름.
- **외부 API 자동 연동 시도 금지**. fflogs/XIVAPI 다 안 씀.
- 멤버는 여러 공대 동시 소속 가능. 권한 체크는 항상 `(userId, partyId)` 쌍.
- PWA·푸시·service worker 손대지 말 것. 그냥 웹사이트.

## 보안 원칙

- Discord OAuth만 사용. 비밀번호 인증 절대 만들지 않는다.
- Firestore 보안 규칙은 모든 새 컬렉션에 작성 후 배포.
- 사용자 입력은 다 검증, 마크다운은 sanitize 통과.
- 비공개 정보(출석 통계 등)는 권한 명시적 체크.
- Firebase App Check + 예산 알림 필수.

## 작업 시 우선 확인

새 기능 작업 전:
1. `TODO.md`에서 현재 단계(MVP/V1/Nice to have) 확인
2. 관련 도메인 타입이 `/types`에 이미 있는지 확인
3. **MVP 범위 밖이면 일단 멈추고 의논**. 스코프 크리프 경계.
